import { moduleFor, test } from 'ember-qunit';

moduleFor('view:display-magazine', 'Unit | View | display magazine');

// Replace this with your real tests.
test('it exists', function(assert) {
  var view = this.subject();
  assert.ok(view);
});
